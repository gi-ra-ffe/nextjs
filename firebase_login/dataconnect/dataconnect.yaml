specVersion: "v1beta"
serviceId: "firebaselogin"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "firebaselogin-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
